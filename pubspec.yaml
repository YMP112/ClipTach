name: cliptach
description: ClipTach - Focused object extraction with transparent background export.
publish_to: "none"
version: 1.2.0+2

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  archive: ^3.6.1
  file_picker: ^8.1.2
  shared_preferences: ^2.3.3
  path: ^1.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
